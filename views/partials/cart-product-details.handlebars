<div class="cart-item">
    <a href="/products/{{product_id}}">
        <img src="{{product_img}}" class="cart-item-image"></img>
        <div class="cart-item-desc">
            <h2>{{product_name}}</h2>
            <hr>
            <p class="product-description"><b>Description:</b>&nbsp;{{description}}</p>
            <p class="product-price"><b>Price:</b>&nbsp;${{price}}</p>
            <p class="product-stock"><b>Stock:</b>&nbsp;{{stock}}</p>
            {{!-- TO DO LATER --}}
            {{!-- <p>Quantity to Buy</p> --}}
        </div>
    </a>
    <div class="cart-item-options">
        <button id="cart-item-delete" data-id={{product_id}}>Delete</button>
        <button id="move-to-wishlist" data-id={{product_id}}>Move to Wishlist</button>
    </div>
</div>